<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bgWhite"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp">

    <!-- Hexagon/Icon Anchor -->
    <ImageView
        android:id="@+id/widget_logo"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:src="@android:drawable/ic_lock_lock"
        android:contentDescription="Locked Asset"
        android:tint="@color/primaryGreen" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Secure Portal Locked"
        android:textColor="@color/colblack"
        android:textSize="16sp"
        android:textStyle="bold"
        android:gravity="center" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Log in to track your transactions and sync your tree data."
        android:textColor="#999999"
        android:textSize="11sp"
        android:gravity="center" />
</LinearLayout>